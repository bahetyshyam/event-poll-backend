import user from "./User";
import group from "./Group";
import event from "./Event";
import response from "./Response";

//Import statements of all other models  and later export it all down in the end.

//All imported models will be added to the object below and exported.
const models = { user, group, event, response };

export default models;
